export const API = '/api';

export const MAPS = `${API}/maps`;
export const CHARACTERS = `${API}/characters`;
export const CHARACTER_EXISTS_ON = `${CHARACTERS}/exists-on`;
export const USERS = `${API}/users`;
export const SCORES = `${API}/scores`;